// The gpay module: the Google Pay Takeout index and enriching statement rows
// with it. Import from here rather than from the file.
export { enrichWithGPay, parseGPayHtml, type GPayIndex } from "./GPayIndex.js";
