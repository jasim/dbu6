/*
 * Setup's URL. `/setup` is card 1 of the first run, the chart
 * (ChartCard): books with no accounts pick their chart there.
 */

/** Card 1, the chart: first run only. */
export const SETUP_ROUTE = "/setup";
