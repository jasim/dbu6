node_modules
.DS_Store
dist
.env*
!template/.env.example
mise.toml
mise.local.toml
.mise.local.toml
*.tsbuildinfo
# Tarballs from `pnpm pack:verify` and `npm pack`
*.tgz
# This repository is not a project and holds no books, but a database or a
# user-config/ put here by mistake must never be committed.
data
/user-config
.vite
*.log
.idea
.pnpm-store
tmp
*.pyc
.package-source-switch.json
