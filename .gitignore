node_modules
.DS_Store
dist
.env*
!.env.development.example
!template/.env.example
mise.toml
mise.local.toml
.mise.local.toml
*.tsbuildinfo
# Tarballs from `pnpm pack:verify` and `npm pack`
*.tgz
# The SQLite database
data
# The user's own mappings, presets and prompts (user-config.example/ is the
# tracked template)
/user-config
.vite
*.log
.idea
.pnpm-store
tmp
*.pyc
.package-source-switch.json
